# Cloud features — disabled by default.
# The app works fully offline when VITE_ENABLE_CLOUD=false.
VITE_ENABLE_CLOUD=false

# Backend API base URL (only used when VITE_ENABLE_CLOUD=true).
# Dev default: http://localhost:8081
VITE_API_BASE_URL=http://localhost:8081
